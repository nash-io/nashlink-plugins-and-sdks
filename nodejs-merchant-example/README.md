# Nash Link Merchant backend & frontend example

Install dependencies, build and start:

    yarn install
    yarn build
    yarn start

Now you can visit the example frontend at http://localhost:3000

More commands:

    yarn lint
    yarn test
